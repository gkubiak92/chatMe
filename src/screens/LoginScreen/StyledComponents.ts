import styled from 'styled-components/native';

const LoginContainer = styled.View`
  flex: 1;
  justify-content: space-evenly;
  align-items: center;
  background-color: white;
`;

const LoginImage = styled.Image`
  height: 260px;
`;

export default { LoginContainer, LoginImage };
