import styled from 'styled-components/native';

const LogoContainer = styled.View`
  justify-content: center;
  align-items: center;
`;

const LogoText = styled.Text`
  font-size: 48px;
`;

export default { LogoContainer, LogoText };
